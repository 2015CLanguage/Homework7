#include<stdio.h>
#include<math.h>
float d,x1,x2,p,q;
void bigger_than_zero(float a,float b);
void zero(float a,float b);
void smaller_than_zero(float a,float b);
int main()
{
	float a,b,c;
	printf("please write down a,b,c: \n");
	scanf("%f,%f,%f",&a,&b,&c);
	printf("equation:%f*x*x+%f*x+%f",a,b,c);
	printf("\n");
	d=b*b-4*a*c;
	if(d>0)
  {
      bigger_than_zero(a,b);
      printf("x1=%f,x2=%f\n",x1,x2);
	}
	else if(d=0)
	{
		 zero(a,b);
		 printf("x1=%f,x2=%f\n",x1,x2);
	}
	else
	{
		 smaller_than_zero(a,b);
		 printf("x1=%f+%fi,x2=%f-%fi\n",x1,x2);
	}
	return 0;
}
void bigger_than_zero(float a,float b)
{
	x1=(-b+sqrt(d))/(2*a);
	x2=(-b-sqrt(d))/(2*a);	
}
void zero(float a,float b)
{
	x1=(-b+sqrt(d))/(2*a);
	x2=(-b-sqrt(d))/(2*a);
}
void smaller_than_zero(float a,float b)
{
	p=-b/(2*a);
	q=sqrt(-d)/(2-a);
}
